<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<title>{{ pack.title }}</title>
<style>{{ css }}</style>
{% if theme_override_css %}<style>{{ theme_override_css }}</style>{% endif %}
</head>
<body class="theme-{{ 'custom' if pack.theme_color else pack.theme }}">
{% import "_ders_govde.html.j2" as ders %}
{{ ders.course(pack, ctx) }}
</body>
</html>
